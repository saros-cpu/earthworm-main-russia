{
  "name": "@earthworm/schema",
  "version": "1.0.2",
  "description": "[DEPRECATED] Legacy Drizzle schema - replaced by backend/ (Spring Data JPA + Flyway). apps/api no longer exists.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "author": "cuixiaorui",
  "scripts": {
    "build": "tsup",
    "release": "pnpm publish --registry http://your-private-registry:4873/ --no-git-checks"
  },
  "dependencies": {
    "@paralleldrive/cuid2": "2.2.2",
    "drizzle-kit": "^0.23.0",
    "drizzle-orm": "^0.32.0"
  }
}
